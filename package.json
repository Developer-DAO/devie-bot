{
  "engines": {
    "node": "16.6.0"
  },
  "scripts": {
    "fmt": "eslint . --fix",
    "lint": "tsc --noEmit && eslint .",
    "deploy:commands": "ts-node deploy-commands.ts",
    "deploy:client": "heroku deploy",
    "dev": "ts-node index.ts",
    "build": "tsc",
    "postinstall": "tsc"
  },
  "dependencies": {
    "@discordjs/builders": "^0.8.1",
    "@discordjs/rest": "^0.1.0-canary.0",
    "airtable": "^0.11.1",
    "discord-api-types": "^0.24.0",
    "discord.js": "^13.3.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.2.0",
    "@typescript-eslint/parser": "^5.2.0",
    "eslint": "^8.1.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  }
}
